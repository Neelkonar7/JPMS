{
  "name": "jpms",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "staging": "npx playwright test smoketest.spec.js --headed --project=chromium --workers=1",
    "production": "npx playwright test smoketest.spec.js --headed --project=chromium_prod --workers=1 --grep-invert @skip",
    "stg_skip_reg_checkout" : "npx playwright test smoketest.spec.js --headed --project=chromium --workers=1 --grep-invert @skip"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@faker-js/faker": "^8.3.1",
    "@playwright/test": "^1.41.2",
    "@types/node": "^20.10.5"
  }
}
